# CI di Pistoia.app.
#
#   controlli ─┐  (lint · typecheck · unit · audit · drift — non blocca nessuno)
#   build ─────┼──► e2e         (Playwright, bloccante dal 2026-08-06)
#              └──► lighthouse  (soglie misurate, bloccante dal 2026-08-06)
#
# Quattro job, tutti bloccanti: se uno è rosso, il workflow è rosso. Nessuno
# però può rendere INVISIBILE un altro — il perché è scritto sopra `controlli`,
# ed è costato quattro commit senza misure.
name: CI

on:
  push:
    branches: [main, "feat/**", "fix/**"]
  pull_request:

env:
  # Valori fittizi sufficienti per build e test: nessun segreto reale in CI.
  DATABASE_URL: "file:./prisma/dev.db"
  SESSION_SECRET: "ci-only-secret-abcdefghijklmnopqrstuvwxyz012345"

  # Su un runner nuovo la versione di pnpm fissata da `packageManager` non è in
  # cache, quindi corepack la scarica al primo uso. Questa variabile toglie la
  # domanda interattiva di conferma, che senza TTY sarebbe un rosso senza
  # spiegazione.
  #
  # ⚠️ NON è la soluzione del problema della working directory, e non va letta
  # così: quello è risolto dal passo `Percorso dello store di pnpm`, che gira
  # dentro `pistoia-dashboard` — cioè dove il pin `packageManager` esiste
  # davvero. Questa riga evita soltanto un prompt.
  COREPACK_ENABLE_DOWNLOAD_PROMPT: "0"

defaults:
  run:
    working-directory: pistoia-dashboard

jobs:
  # ══════════════════════════════════════════════════════════════════════════
  # 🔴 DUE JOB DOVE PRIMA CE N'ERA UNO (2026-08-15)
  #
  # `quality` mescolava due responsabilità — i CONTROLLI (lint, typecheck, unit,
  # audit, drift) e la PRODUZIONE della build — ed `e2e` e `lighthouse` avevano
  # entrambi `needs: quality`.
  #
  # Il costo, misurato: dal 14 al 15 agosto un'advisory `high` su una transitiva
  # ha reso rosso `quality`, e con lei **E2E e Lighthouse non sono falliti: sono
  # stati SALTATI**, per quattro commit. `skipped` non è rosso, e in un log da
  # 400 righe non lo legge nessuno. Nel frattempo quattro test erano rotti e
  # nessuno lo sapeva; l'ultima misura di prestazioni era del 13/08.
  #
  # ⚠️ La cura NON è `if: always()`, ed è il punto. Né `e2e` né `lighthouse`
  # consumano l'artefatto di `quality`: installano per conto proprio e
  # `lighthouse` ricostruisce da zero. Quel `needs` non era una dipendenza di
  # dati, era un cancello messo dove capitava. Separare le responsabilità dice
  # la verità sulla struttura, e i cinque comportamenti che vogliamo escono da
  # soli dalla semantica di `needs`, senza scavalcarla:
  #
  #   1. tutto verde              → E2E e Lighthouse girano
  #   2. lint/audit rossi, build ok → girano LO STESSO (dipendono da `build`)
  #   3. build fallita            → saltati (non c'è niente da provare)
  #   4. E2E o Lighthouse rossi   → il workflow è rosso
  #   5. esecuzione annullata     → non parte lavoro inutile
  #
  # `controlli` non dipende da nulla e non blocca nessuno: se è rosso, il
  # workflow è rosso — ma gli altri cancelli hanno detto la loro nella stessa
  # passata. L'obiettivo è vedere PIÙ errori per giro, non nasconderne uno.
  # ══════════════════════════════════════════════════════════════════════════
  controlli:
    name: Lint · Typecheck · Test · Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # ────────────────────────────────────────────────────────────────────
      # Node, poi la cache dello store di pnpm calcolata ESPLICITAMENTE.
      #
      # 🔴 Perché NON `cache: pnpm` di `setup-node`: quella è un'azione
      # JavaScript, e `defaults.run.working-directory` **non la tocca**.
      # Interrogherebbe `pnpm store path` dalla RADICE del repository, dove non
      # c'è `package.json` — quindi corepack non troverebbe il pin
      # `packageManager` del sottoprogetto e userebbe una versione propria.
      # Il percorso si calcola invece con un passo `run` dentro
      # `pistoia-dashboard`, che è l'unico posto dove il pin esiste.
      #
      # Le azioni sono UFFICIALI e **pinnate al commit**, non al tag: un tag si
      # può rispostare, uno SHA no.
      #
      # ⚠️ Le tre azioni girano su runtime `node24`, che pretende un runner
      # >= 2.327.1: qui sono tutti GitHub-hosted (`ubuntu-latest`, zero runner
      # self-hosted registrati sul repository), quindi si aggiornano da soli.
      # ────────────────────────────────────────────────────────────────────
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          # La versione Node DEL PROGETTO resta 22: il runtime node24 con cui
          # gira l'azione è cosa sua, e non tocca build né test.
          node-version: 22
          # Rinuncia ESPLICITA alla cache interna dell'azione: da v5 l'input
          # esiste, quindi la si nega per nome invece che per omissione.
          # L'unico meccanismo di cache resta `actions/cache` qui sotto.
          package-manager-cache: false

      - name: Percorso dello store di pnpm
        id: pnpm-store
        working-directory: pistoia-dashboard
        shell: bash
        run: echo "path=$(corepack pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      - uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ steps.pnpm-store.outputs.path }}
          key: pnpm-${{ runner.os }}-${{ hashFiles('pistoia-dashboard/pnpm-lock.yaml') }}
          # Fallback: riusa lo store content-addressed precedente quando il
          # lockfile cambia, invece di ripartire da freddo. NON allenta niente —
          # la correttezza la decide `--frozen-lockfile`, che pretende comunque
          # il lockfile esatto. Sicuro perché questo workflow non usa
          # `pull_request_target` né altri contesti privilegiati con codice non
          # fidato (verificato).
          restore-keys: |
            pnpm-${{ runner.os }}-

      - name: Installa dipendenze (esegue anche prisma generate)
        run: corepack pnpm install --frozen-lockfile

      - name: Lint
        run: corepack pnpm lint

      - name: Typecheck
        run: corepack pnpm typecheck

      - name: Unit test (Vitest)
        run: corepack pnpm test

      # Audit delle dipendenze — traccia «Qualità continua».
      #
      # BLOCCANTE dal 2026-08-06. Fino al giorno prima era `npm audit || true`,
      # e la ragione era dichiarata: restavano avvisi che aspettavano una
      # decisione, e un cancello rosso per qualcosa che si è scelto di non
      # chiudere smette di essere letto. Quella ragione è caduta: la C-2 ha
      # portato l'audit da 12 a **zero**, e lo zero ha retto.
      #
      # `--audit-level=high` e non `pnpm audit` nudo: la soglia è sul CODICE DI
      # USCITA, non sul referto — il report esce comunque intero nei log, quindi
      # un avviso `moderate` resta VISIBILE senza far cadere la pipeline. È la
      # stessa logica del commento di prima, applicata al contrario: qui il
      # rosso deve significare «è successo qualcosa di serio», altrimenti smette
      # di essere letto anche questo.
      #
      # Le dipendenze di sviluppo sono incluse per default (le toglie `--prod`), ed è voluto: il Dockerfile fa
      # `pnpm install --frozen-lockfile`, quindi le dipendenze di sviluppo finiscono
      # nell'immagine di produzione e vanno auditate come le altre.
      - name: Audit dipendenze
        run: corepack pnpm audit --audit-level=high

      - name: Drift migrazioni (schema ↔ migrations)
        # exit 2 se lo schema diverge dalle migrazioni committate.
        run: corepack pnpm exec prisma migrate diff --from-migrations prisma/migrations --to-schema prisma/schema.prisma --exit-code

  # Il job che produce l'artefatto, e l'unico da cui abbia senso far dipendere
  # chi misura: se il codice non compila, non c'è niente da provare né da
  # cronometrare. Gira in parallelo a `controlli`, che non lo blocca.
  build:
    name: Build di produzione
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # ────────────────────────────────────────────────────────────────────
      # Node, poi la cache dello store di pnpm calcolata ESPLICITAMENTE.
      #
      # 🔴 Perché NON `cache: pnpm` di `setup-node`: quella è un'azione
      # JavaScript, e `defaults.run.working-directory` **non la tocca**.
      # Interrogherebbe `pnpm store path` dalla RADICE del repository, dove non
      # c'è `package.json` — quindi corepack non troverebbe il pin
      # `packageManager` del sottoprogetto e userebbe una versione propria.
      # Il percorso si calcola invece con un passo `run` dentro
      # `pistoia-dashboard`, che è l'unico posto dove il pin esiste.
      #
      # Le azioni sono UFFICIALI e **pinnate al commit**, non al tag: un tag si
      # può rispostare, uno SHA no.
      #
      # ⚠️ Le tre azioni girano su runtime `node24`, che pretende un runner
      # >= 2.327.1: qui sono tutti GitHub-hosted (`ubuntu-latest`, zero runner
      # self-hosted registrati sul repository), quindi si aggiornano da soli.
      # ────────────────────────────────────────────────────────────────────
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          # La versione Node DEL PROGETTO resta 22: il runtime node24 con cui
          # gira l'azione è cosa sua, e non tocca build né test.
          node-version: 22
          # Rinuncia ESPLICITA alla cache interna dell'azione: da v5 l'input
          # esiste, quindi la si nega per nome invece che per omissione.
          # L'unico meccanismo di cache resta `actions/cache` qui sotto.
          package-manager-cache: false

      - name: Percorso dello store di pnpm
        id: pnpm-store
        working-directory: pistoia-dashboard
        shell: bash
        run: echo "path=$(corepack pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      - uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ steps.pnpm-store.outputs.path }}
          key: pnpm-${{ runner.os }}-${{ hashFiles('pistoia-dashboard/pnpm-lock.yaml') }}
          # Fallback: riusa lo store content-addressed precedente quando il
          # lockfile cambia, invece di ripartire da freddo. NON allenta niente —
          # la correttezza la decide `--frozen-lockfile`, che pretende comunque
          # il lockfile esatto. Sicuro perché questo workflow non usa
          # `pull_request_target` né altri contesti privilegiati con codice non
          # fidato (verificato).
          restore-keys: |
            pnpm-${{ runner.os }}-

      - name: Installa dipendenze (esegue anche prisma generate)
        run: corepack pnpm install --frozen-lockfile

      # Resta qui com'era: il build non legge il database, ma la preparazione
      # costa secondi e toglierla significherebbe scoprire per caso, un giorno,
      # che una rotta lo leggeva davvero.
      - name: Prepara database (migrate + seed)
        run: corepack pnpm exec prisma migrate deploy && corepack pnpm exec tsx prisma/seed.ts

      - name: Build di produzione
        run: corepack pnpm build

  e2e:
    name: E2E (Playwright)
    runs-on: ubuntu-latest
    needs: build
    # BLOCCANTE dal 2026-08-06. Nasceva `continue-on-error` «finché la suite non
    # è stata rodata in CI»: è rodata — 48 test verdi in locale, e verde in CI su
    # ogni passata da settimane. La riga è l'ultima della famiglia chiusa oggi
    # (audit, Lighthouse): un cancello dichiarato non bloccante «per ora» che ci
    # è rimasto, e che intanto lasciava passare un E2E rotto senza fermare
    # nessuno. È l'unico cancello che prova i percorsi veri — login, voto,
    # moderazione — e non può essere quello che vale meno di tutti.
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # ────────────────────────────────────────────────────────────────────
      # Node, poi la cache dello store di pnpm calcolata ESPLICITAMENTE.
      #
      # 🔴 Perché NON `cache: pnpm` di `setup-node`: quella è un'azione
      # JavaScript, e `defaults.run.working-directory` **non la tocca**.
      # Interrogherebbe `pnpm store path` dalla RADICE del repository, dove non
      # c'è `package.json` — quindi corepack non troverebbe il pin
      # `packageManager` del sottoprogetto e userebbe una versione propria.
      # Il percorso si calcola invece con un passo `run` dentro
      # `pistoia-dashboard`, che è l'unico posto dove il pin esiste.
      #
      # Le azioni sono UFFICIALI e **pinnate al commit**, non al tag: un tag si
      # può rispostare, uno SHA no.
      #
      # ⚠️ Le tre azioni girano su runtime `node24`, che pretende un runner
      # >= 2.327.1: qui sono tutti GitHub-hosted (`ubuntu-latest`, zero runner
      # self-hosted registrati sul repository), quindi si aggiornano da soli.
      # ────────────────────────────────────────────────────────────────────
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          # La versione Node DEL PROGETTO resta 22: il runtime node24 con cui
          # gira l'azione è cosa sua, e non tocca build né test.
          node-version: 22
          # Rinuncia ESPLICITA alla cache interna dell'azione: da v5 l'input
          # esiste, quindi la si nega per nome invece che per omissione.
          # L'unico meccanismo di cache resta `actions/cache` qui sotto.
          package-manager-cache: false

      - name: Percorso dello store di pnpm
        id: pnpm-store
        working-directory: pistoia-dashboard
        shell: bash
        run: echo "path=$(corepack pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      - uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ steps.pnpm-store.outputs.path }}
          key: pnpm-${{ runner.os }}-${{ hashFiles('pistoia-dashboard/pnpm-lock.yaml') }}
          # Fallback: riusa lo store content-addressed precedente quando il
          # lockfile cambia, invece di ripartire da freddo. NON allenta niente —
          # la correttezza la decide `--frozen-lockfile`, che pretende comunque
          # il lockfile esatto. Sicuro perché questo workflow non usa
          # `pull_request_target` né altri contesti privilegiati con codice non
          # fidato (verificato).
          restore-keys: |
            pnpm-${{ runner.os }}-

      - name: Installa dipendenze
        run: corepack pnpm install --frozen-lockfile

      - name: Prepara database (migrate + seed)
        run: corepack pnpm exec prisma migrate deploy && corepack pnpm exec tsx prisma/seed.ts

      - name: Installa Chromium
        run: corepack pnpm exec playwright install --with-deps chromium

      - name: Test end-to-end
        run: corepack pnpm test:e2e

      - name: Carica il report in caso di fallimento
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: pistoia-dashboard/playwright-report/
          retention-days: 7

  lighthouse:
    # Il nome diceva «misura, non giudica» ed era vero fino al 2026-08-06:
    # adesso giudica, e il documento non può restare indietro sul codice.
    name: Lighthouse (soglie misurate)
    runs-on: ubuntu-latest
    needs: build
    # BLOCCANTE dal 2026-08-06. Era `continue-on-error: true` «finché non
    # esiste una soglia misurata»: adesso esiste, e sta in `lighthouserc.js`
    # con la tabella della prima misura vera accanto. Le soglie stanno cinque
    # punti sotto il minimo osservato, perché una soglia appoggiata al minimo
    # lampeggia al primo rumore della macchina condivisa.
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      # ────────────────────────────────────────────────────────────────────
      # Node, poi la cache dello store di pnpm calcolata ESPLICITAMENTE.
      #
      # 🔴 Perché NON `cache: pnpm` di `setup-node`: quella è un'azione
      # JavaScript, e `defaults.run.working-directory` **non la tocca**.
      # Interrogherebbe `pnpm store path` dalla RADICE del repository, dove non
      # c'è `package.json` — quindi corepack non troverebbe il pin
      # `packageManager` del sottoprogetto e userebbe una versione propria.
      # Il percorso si calcola invece con un passo `run` dentro
      # `pistoia-dashboard`, che è l'unico posto dove il pin esiste.
      #
      # Le azioni sono UFFICIALI e **pinnate al commit**, non al tag: un tag si
      # può rispostare, uno SHA no.
      #
      # ⚠️ Le tre azioni girano su runtime `node24`, che pretende un runner
      # >= 2.327.1: qui sono tutti GitHub-hosted (`ubuntu-latest`, zero runner
      # self-hosted registrati sul repository), quindi si aggiornano da soli.
      # ────────────────────────────────────────────────────────────────────
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          # La versione Node DEL PROGETTO resta 22: il runtime node24 con cui
          # gira l'azione è cosa sua, e non tocca build né test.
          node-version: 22
          # Rinuncia ESPLICITA alla cache interna dell'azione: da v5 l'input
          # esiste, quindi la si nega per nome invece che per omissione.
          # L'unico meccanismo di cache resta `actions/cache` qui sotto.
          package-manager-cache: false

      - name: Percorso dello store di pnpm
        id: pnpm-store
        working-directory: pistoia-dashboard
        shell: bash
        run: echo "path=$(corepack pnpm store path --silent)" >> "$GITHUB_OUTPUT"

      - uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ steps.pnpm-store.outputs.path }}
          key: pnpm-${{ runner.os }}-${{ hashFiles('pistoia-dashboard/pnpm-lock.yaml') }}
          # Fallback: riusa lo store content-addressed precedente quando il
          # lockfile cambia, invece di ripartire da freddo. NON allenta niente —
          # la correttezza la decide `--frozen-lockfile`, che pretende comunque
          # il lockfile esatto. Sicuro perché questo workflow non usa
          # `pull_request_target` né altri contesti privilegiati con codice non
          # fidato (verificato).
          restore-keys: |
            pnpm-${{ runner.os }}-

      - name: Installa dipendenze
        run: corepack pnpm install --frozen-lockfile

      - name: Prepara database (migrate + seed)
        run: corepack pnpm exec prisma migrate deploy && corepack pnpm exec tsx prisma/seed.ts

      # Si misura la build di produzione: in sviluppo il bundle non è
      # minificato e i numeri non somigliano a quelli di nessun cittadino.
      - name: Build di produzione
        run: corepack pnpm build

      # `@lhci/cli` NON è una dipendenza del progetto, ed è una scelta: porta
      # **285 pacchetti** (e cinque avvisi propri, fra cui `tmp` high) che il
      # `Dockerfile` si porterebbe in produzione, perché installa le dipendenze
      # di sviluppo insieme alle altre. Uno strumento di misura che gira solo in
      # CI non deve entrare nell'immagine del servizio. La versione è
      # **pinnata**: `pnpm dlx` senza versione scaricherebbe l'ultima, cioè
      # codice non deciso.
      - name: Lighthouse CI
        run: corepack pnpm dlx @lhci/cli@0.15.1 autorun

      # I PUNTEGGI NEL LOG, e non è un di più: senza `assert` in
      # `lighthouserc.js`, `lhci autorun` si ferma a `collect` e **non stampa
      # nessuna tabella**. Fino al 2026-08-06 questo job girava, scriveva 12
      # referti sul disco, e non lasciava un solo numero leggibile da nessuna
      # parte — mentre si diceva «le soglie si scrivono dopo aver guardato le
      # prime passate». Non c'era niente da guardare.
      #
      # Legge il manifest e stampa la MEDIANA per URL, che è la corsa che
      # `lhci` marca `isRepresentativeRun`.
      - name: Punteggi (mediana delle tre passate)
        if: always()
        run: |
          node -e '
            const fs = require("fs");
            const m = JSON.parse(fs.readFileSync(".lighthouseci/manifest.json", "utf8"));
            const pad = (s, n) => String(s).padEnd(n);
            console.log(pad("URL", 46) + ["perf", "a11y", "best", "seo"].map((h) => pad(h, 7)).join(""));
            for (const r of m.filter((x) => x.isRepresentativeRun)) {
              const s = r.summary;
              console.log(
                pad(r.url.replace("http://localhost:3000", ""), 46) +
                [s.performance, s.accessibility, s["best-practices"], s.seo]
                  .map((v) => pad(Math.round(v * 100), 7)).join("")
              );
            }
          '

      - name: Carica i referti
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lighthouse-report
          path: pistoia-dashboard/.lighthouseci/
          # ⚠️ SENZA QUESTA RIGA NON SI CARICA NIENTE, e il job dice «success».
          # `.lighthouseci` comincia con un punto, e da `upload-artifact@v4.4`
          # i file nascosti sono esclusi per default: il passo si limitava a un
          # `##[warning] No files were found`, che in un log da 400 righe non lo
          # legge nessuno. Verificato sulla passata del 2026-08-05: dodici
          # referti scritti, zero caricati, esito verde.
          include-hidden-files: true
          retention-days: 7
